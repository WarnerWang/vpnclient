<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="vericationCodeInput">

        <attr name="box" format="integer" />
        <attr name="child_h_padding" format="dimension" />
        <attr name="child_v_padding" format="dimension" />
        <attr name="child_width" format="dimension" />
        <attr name="child_height" format="dimension" />
        <attr name="padding" format="dimension" />
        <attr name="box_bg_focus" format="reference" />
        <attr name="box_bg_normal" format="reference" />
        <attr name="inputType" format="string" />
    </declare-styleable>

    <declare-styleable name="BGAProgressBar">
        <!-- 进度条样式，默认值为 system -->
        <attr name="bga_pb_mode" format="enum">
            <!-- 系统自带样式 -->
            <enum name="system" value="0" />
            <!-- 水平进度条 -->
            <enum name="horizontal" value="1" />
            <!-- 圆形进度条 -->
            <enum name="circle" value="2" />
            <!-- 彗星进度条 -->
            <enum name="comet" value="3" />
            <!-- 水波纹进度条 -->
            <enum name="wave" value="4" />
        </attr>
        <!-- 文字颜色，默认值为 #70A800 -->
        <attr name="bga_pb_textColor" format="reference|color" />
        <!-- 文字大小，默认值为 10sp -->
        <attr name="bga_pb_textSize" format="dimension" />
        <!-- 文字和进度条之间的间距，默认值为 4dp -->
        <attr name="bga_pb_textMargin" format="dimension" />
        <!-- 已完成进度的颜色，默认值为 #70A800 -->
        <attr name="bga_pb_reachedColor" format="reference|color" />
        <!-- 已完成进度的高度，默认值为 2dp -->
        <attr name="bga_pb_reachedHeight" format="dimension" />
        <!-- 未完成进度的颜色，默认值为 #CCCCCC -->
        <attr name="bga_pb_unReachedColor" format="reference|color" />
        <!-- 未完成进度的高度，默认值为 1dp -->
        <attr name="bga_pb_unReachedHeight" format="dimension" />
        <!-- 是否为圆角，默认值为 false -->
        <attr name="bga_pb_isCapRounded" format="boolean" />
        <!-- 是否隐藏文字，默认值为 false -->
        <attr name="bga_pb_isHiddenText" format="boolean" />
        <!-- 圆形进度条半径，默认值为 16dp -->
        <attr name="bga_pb_radius" format="dimension" />
        <attr name="bga_pb_center_text" format="string" />
        <attr name="bga_pb_sub_text" format="string" />
        <attr name="bga_pb_sub_text_size" format="dimension" />
        <attr name="bga_pb_header_text" format="string" />
        <attr name="bga_pb_header_text_size" format="dimension" />
        <attr name="bga_pb_header_text_color" format="reference|color" />
    </declare-styleable>

    <declare-styleable name="LeanTextView">
        <attr name="degree" format="dimension" />
    </declare-styleable>

    <declare-styleable name="WJTextView">

        <attr name="wj_solid_color" format="color" />
        <attr name="wj_border_color" format="color" />
        <attr name="wj_border_width" format="dimension" />
        <attr name="wj_corner_radius" format="dimension" />
        <attr name="wj_top_left_radius" format="dimension" />
        <attr name="wj_top_right_radius" format="dimension" />
        <attr name="wj_bottom_left_radius" format="dimension" />
        <attr name="wj_bottom_right_radius" format="dimension" />
        <attr name="wj_gradient_start_color" format="color" />
        <attr name="wj_gradient_end_color" format="color" />
        <attr name="wj_gradient_center_color" format="color" />
        <attr name="wj_gradient_orientation" format="enum">
            <!-- 从上到下 -->
            <enum name="TOP_BOTTOM" value="0" />
            <!-- 右上到左下 -->
            <enum name="TR_BL" value="1" />
            <!-- 从右到左 -->
            <enum name="RIGHT_LEFT" value="2" />
            <!-- 从右下到左上 -->
            <enum name="BR_TL" value="3" />
            <!-- 从下到上 -->
            <enum name="BOTTOM_TOP" value="4" />
            <!-- 从左下到右上 -->
            <enum name="BL_TR" value="5" />
            <!-- 从左到右 -->
            <enum name="LEFT_RIGHT" value="6" />
            <!-- 从左上到右下 -->
            <enum name="TL_BR" value="7" />
        </attr>

    </declare-styleable>

    <declare-styleable name="RoundAngleImageView">
        <attr name="roundWidth" format="dimension" />
        <attr name="roundHeight" format="dimension" />
    </declare-styleable>


</resources>